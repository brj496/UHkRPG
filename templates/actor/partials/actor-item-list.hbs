{{!-- TODO: A card based item list for the more generic item lists. --}}

{{!-- This needs to have options for all of the items. So it needs to take an input for keys to display. Every item does have a description. --}}

{{!--
    Options:
        This could either take the item type, and it adjusts to
        what each item type needs, but added a new item type would
        be a bit of a pain.

        Or it could take a list of attributes it needs to display.
        Different Attributes are display in different places.

        Pass it a type, then pass it to a different partial/function/helper function?

 --}}